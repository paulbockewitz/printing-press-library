{
  "manifest_version": "0.3",
  "name": "conduyt-crm-pp-mcp",
  "display_name": "Conduyt",
  "version": "4.2.0",
  "description": "AI-native CRM for the terminal — manage contacts, deals, pipelines, automations, email sequences, SMS drip campaigns, invoices, and 70+ resource types. Local SQLite sync, compound insight queries (pipeline health, deal velocity, email stats), and MCP code-orchestration for agent integration.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/conduyt-crm-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/conduyt-crm-pp-mcp",
      "args": [],
      "env": {
        "CONDUYT_CRM_TOKEN": "${user_config.conduyt_crm_token}"
      }
    }
  },
  "user_config": {
    "conduyt_crm_token": {
      "type": "string",
      "title": "CONDUYT_CRM_TOKEN",
      "description": "Bearer token for authenticating with the Conduyt CRM API.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
