{
  "schema_version": 1,
  "applied_at": "2026-05-16",
  "base_run_id": "20260509-180956",
  "base_printing_press_version": "4.2.1",
  "patches": [
    {
      "id": "pagination-cursor-wiring",
      "summary": "Wire pagination.nextCursor and pagination.hasMore paths into list commands.",
      "reason": "The OpenAPI parser (parser.go:4295) only walks top-level response properties when scanning for next_cursor / has_more. Tella's REST envelope nests them under {pagination:{...}}, so Pagination.NextCursorPath and HasMoreField were emitted as empty strings and the --all loop exited after page 1.",
      "files": [
        "internal/cli/videos_list.go",
        "internal/cli/playlists_list.go"
      ],
      "validated_outcome": "videos list --all returns all 232 videos across 16 cursor-followed pages (was 15)."
    },
    {
      "id": "format-flag-routing-and-helpers",
      "summary": "Add unwrapResultsArray, unwrapSingleEnvelope, printQuiet, printPlain helpers; route --csv/--quiet/--plain above the piped JSON gate; unwrap envelopes before --select/--compact.",
      "reason": "The generated gate `flags.asJSON || !isTerminal()` short-circuited explicit format flags when stdout was piped (the agent / shell case). printOutputWithFlags also fed CSV/plain renderers an envelope-shaped JSON instead of a flat array. --quiet was silent despite docs saying 'one value per line'. --plain had no renderer at all.",
      "files": ["internal/cli/helpers.go", "internal/cli/videos_list.go"],
      "validated_outcome": "videos list --csv --select id,name | head -3 emits CSV header + 2 rows; videos list --quiet --select id emits one ID per line; videos list --plain --select id,views emits tab-separated rows; videos/playlists/clips get --select id,name returns the populated {id,name} object."
    },
    {
      "id": "usage-exit-codes",
      "summary": "Replace `if len(args) == 0 { return cmd.Help() }` with usageErr; add rejectUnknownSubcommand RunE on parent commands.",
      "reason": "Two related issues: (1) cmd.Help() returns nil so missing positional args silently exited rc=0 in 51 generated commands; (2) parent commands without Run/RunE are non-Runnable in cobra v1.9.1, so `videos garbage` returned flag.ErrHelp (rc=0) before Args validation could run. Fix: make parents Runnable with a RunE that errors on unknown subcommand.",
      "files": ["internal/cli/helpers.go", "internal/cli/playlists.go"],
      "validated_outcome": "playlists update --name x (missing <id>) exits rc=2; videos garbage exits rc=3 with 'unknown command'; bare parent (videos with no subcommand) still exits rc=0 with help; valid subcommands still dispatch."
    },
    {
      "id": "videos-viewed-help-clarity",
      "summary": "Add long-form --help text clarifying that videos viewed sources from the webhook event stream.",
      "reason": "Empty results from videos viewed against a workspace with real views (622 total) was confusing. The rollup is keyed off video.viewed/video.milestone webhook events, not the API counter — empty results don't mean zero views, they mean no view events have hit the inbox yet.",
      "files": ["internal/cli/videos_viewed.go"],
      "validated_outcome": "videos viewed --help now explains the data source and points users to wire a webhook endpoint."
    },
    {
      "id": "add-remove-buffers-composition",
      "summary": "Hand-add `videos clips remove-buffers` and `clips edit-pass --remove-buffers` as a composition of public-API endpoints (GET /clips/{clipId} + GET /silences + POST /cut per head/tail range).",
      "reason": "Tella's Cut panel ships Remove buffers and Find mistakes; live smoke tests against api.tella.com on 2026-05-16 confirmed both endpoints return 404 (also 404: trim-buffers, cut-buffers, detect-mistakes, analyze-mistakes, remove-mistakes) while baseline /remove-fillers returned 200 — neither is exposed on the public API. The Tella web UI uses unofficial internal endpoints on www.tella.tv (frontend API, cookie auth) and prod-stream.tella.tv (AI streaming service, Server-Sent Events), verified via captured HAR. For Remove buffers, the user's stated head+tail semantic is implementable as a composition of documented public endpoints, so it's added here. Find mistakes requires the unofficial SSE service; deferred until Tella publishes a public endpoint or a follow-up PR explicitly scopes unofficial-API support. Side note: clips_edit_pass.go's existing extractSilenceRanges helper doesn't recognize the current public-API silences shape ({startTimeMs, durationMs}); this PR introduces a parallel parseSilencesMs helper for the new --remove-buffers path rather than retrofitting the legacy parser, so the older --trim-silences-gt code path remains unchanged here.",
      "files": [
        "internal/cli/videos_clips_remove-buffers.go",
        "internal/cli/videos_clips.go",
        "internal/cli/clips_edit_pass.go",
        "internal/cli/clips_edit_pass_test.go",
        "internal/cli/root.go",
        "internal/cli/which.go",
        "internal/mcp/tools.go",
        "SKILL.md",
        "README.md"
      ],
      "validated_outcome": "`tella-pp-cli videos clips remove-buffers <vid> <cl> --dry-run --json` returns a plan envelope with `head_cut`/`tail_cut` ranges derived from /silences against /clips/{clipId}.durationSeconds. `tella-pp-cli clips edit-pass --playlist <id> --remove-buffers --dry-run --json` includes `remove-buffers-head` / `remove-buffers-tail` ops per clip when applicable. `--apply` POSTs each range to /v1/videos/{id}/clips/{clipId}/cut. Live verification against the user's test workspace confirmed the underlying endpoints return 200.",
      "deferred_to_upstream": [
        {
          "feature": "find-mistakes",
          "reason": "Web UI uses prod-stream.tella.tv/ai-mistakes/analyze-scene (Server-Sent Events, separate host, separate auth). No public API equivalent (verified 404 on 2026-05-16). Re-evaluate when Tella publishes an endpoint or when scope expands to support the unofficial API."
        }
      ],
      "upstream_issue": "https://github.com/mvanhorn/cli-printing-press/issues/1528"
    }
  ]
}
