// Copyright 2026 mvanhorn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package store

import (
	"context"
	"path/filepath"
	"testing"
	"time"
)

func TestListAwaitingReply(t *testing.T) {
	s, err := Open(filepath.Join(t.TempDir(), "data.db"))
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()
	now := time.Date(2026, 5, 15, 12, 0, 0, 0, time.UTC)
	if _, err := s.UpsertGmailMessages(context.Background(), []StoredMessage{
		{ID: "m1", ThreadID: "t1", AccountEmail: "user@example.com", LabelIDs: []string{"INBOX"}, From: "alice@external.example", To: "user@example.com", Subject: "Need reply", InternalDate: now.Add(-5 * time.Hour).UnixMilli()},
		{ID: "m2", ThreadID: "t2", AccountEmail: "user@example.com", LabelIDs: []string{"INBOX"}, From: "user@example.com", To: "bob@external.example", Subject: "Already replied", InternalDate: now.Add(-6 * time.Hour).UnixMilli()},
		{ID: "m3", ThreadID: "t3", AccountEmail: "user@example.com", LabelIDs: []string{"INBOX"}, From: "teammate@example.com", To: "user@example.com", Subject: "Internal", InternalDate: now.Add(-5 * time.Hour).UnixMilli()},
	}); err != nil {
		t.Fatalf("upsert: %v", err)
	}
	threads, err := s.ListAwaitingReply(context.Background(), AwaitingReplyFilter{
		AccountEmail: "user@example.com",
		Since:        now.Add(-24 * time.Hour),
		MinAge:       4 * time.Hour,
		ExternalOnly: true,
		Now:          now,
	})
	if err != nil {
		t.Fatalf("ListAwaitingReply: %v", err)
	}
	if len(threads) != 1 || threads[0].ThreadID != "t1" {
		t.Fatalf("threads = %+v want t1 only", threads)
	}
}
